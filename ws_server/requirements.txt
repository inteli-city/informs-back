# Lock file de dependências do ws_server. Gerado a partir do
# pyproject.toml com versões transitivas resolvidas pra Python 3.11
# (Debian 12 nativo). Atualização: rodar
#   pip-compile pyproject.toml -o requirements.txt --resolver=backtracking
# ou simplesmente atualizar à mão e rodar pytest.
#
# Resolve text:S8565 — SonarQube exige lock file pra builds reprodutíveis.

# Diretas (idênticas às do pyproject.toml)
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.0.1
boto3==1.35.0
pydantic==2.9.0
python-jose[cryptography]==3.3.0
httpx==0.27.0

# Transitivas relevantes (pinadas pra reprodutibilidade)
# fastapi → starlette
starlette==0.38.5
# pydantic → pydantic-core + annotated-types
pydantic-core==2.23.2
annotated-types==0.7.0
# uvicorn[standard] → httptools, uvloop, watchfiles, websockets, python-dotenv, pyyaml, httptools
httptools==0.6.1
uvloop==0.20.0
watchfiles==0.24.0
python-dotenv==1.0.1
pyyaml==6.0.2
# boto3 → botocore + s3transfer + jmespath
botocore==1.35.0
s3transfer==0.10.2
jmespath==1.0.1
# python-jose[cryptography] → cryptography, ecdsa, pyasn1, rsa
cryptography==43.0.1
ecdsa==0.19.0
pyasn1==0.6.1
rsa==4.9
# httpx → httpcore, anyio, sniffio, certifi, idna, h11
httpcore==1.0.5
anyio==4.6.0
sniffio==1.3.1
certifi==2024.8.30
idna==3.10
h11==0.14.0
# Comuns
typing-extensions==4.12.2
click==8.1.7
six==1.16.0
urllib3==2.2.3
python-dateutil==2.9.0.post0
