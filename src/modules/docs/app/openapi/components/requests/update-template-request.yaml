type: object
required: [name, system, isActive, sections]
properties:
  name: { type: string }
  system: { $ref: "../enums/docs/system.yaml" }
  description: { type: string, nullable: true }
  isActive: { type: boolean }
  sections:
    type: array
    items: { $ref: "../entities/docs/form-template.yaml#/TemplateSection" }
