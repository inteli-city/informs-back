[pytest]
# Tests do ws_server vivem num subprojeto separado com deps próprias
# (fastapi, websockets, httpx, moto). Têm seu próprio job no CI
# (testWsServer no .github/workflows/CI.yml). O pytest da raiz cobre só
# os tests do projeto Lambda.
norecursedirs = tests/ws_server venv .venv iac/cdk.out
